/*
 The Direction is an Enum type which is defined the Direction of GameObject.
 */
using System;

namespace SpaceInvaders
{
    /// <summary>
    /// Direction, is not every game object has all direction, but just define here all situation.
    /// </summary>
    public enum Direction
    {
        Left,
        Right,
        Up,
        Down
    }
}
